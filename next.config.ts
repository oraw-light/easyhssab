import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // HMR/file-watching is intentionally left at Next's default here —
  // the old DISABLE_HMR knob was a Vite dev-server concern, not applicable to Next.
};

export default nextConfig;
